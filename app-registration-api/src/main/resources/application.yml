server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/closed_sarc_db
    username: closed_sarc
    password: closed_sarc
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
